{
  "name": "ConsciousnessProfile",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Description optionnelle (max 1000 caract\u00e8res)"
    },
    "name": {
      "type": "string",
      "description": "Nom du profil de conscience"
    },
    "config": {
      "type": "object",
      "description": "Configuration compl\u00e8te de la conscience sauvegard\u00e9e"
    }
  },
  "required": [
    "name",
    "config"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "created_by": "{{user.email}}"
    },
    "update": {
      "created_by": "{{user.email}}"
    },
    "delete": {
      "created_by": "{{user.email}}"
    }
  }
}